version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: nexaforge-ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
      - ollama_db:/root/.ollama/data

volumes:
  ollama_models:
  ollama_db: